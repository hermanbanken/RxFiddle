// import { parse } from "esprima/src/esprima"
// var TypeScript = require("./typescriptServices")

// class JavascriptSyntaxAnalyser implements ISyntaxAnalyser {
//   public parseSyntax(syntax: string): ObservableTree[] {

//     function isConsoleCall(node: ast: TypeScript.AST) {
//       return (node.type === "CallExpression") &&
//         (node.callee.type === "MemberExpression") &&
//         (node.callee.object.type === "Identifier") &&
//         (node.callee.object.name === "console")
//     }

//     let ast = esprima.parse(syntax)
//     throw new Error("Method not implemented.")
//   }


// }
