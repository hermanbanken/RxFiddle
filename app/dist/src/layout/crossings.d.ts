import { Edge } from "./index";
import { Graph } from "graphlib";
export declare function crossings(vRow: string[], wRow: string[], edges: Edge[]): number;
export declare function order_crossings(order: string[][], g: Graph): number;
