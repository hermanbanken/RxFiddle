import { Direction, ExternalSort } from "./index";
import { Graph } from "graphlib";
export declare function wmedian(ranks: string[][], g: Graph, dir: Direction, externalSort?: ExternalSort): void;
export declare function median(list: number[]): number;
