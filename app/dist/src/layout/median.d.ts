import { Direction } from "./index";
import { Graph } from "graphlib";
export declare function wmedian(ranks: string[][], g: Graph, dir: Direction): void;
export declare function median(list: number[]): number;
